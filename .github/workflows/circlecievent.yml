on:
    pull_request:
      types: [opened]
jobs:
   trigger-circleci:
      runs-on: ubuntu-latest
      steps:
        - name: CircleCI Trigger on Pull Requestss
          id: test1
          uses: CircleCI-Public/trigger-circleci-pipeline-action@v1.0
          env:
            CCI_TOKEN: ${{ secrets.CCI_TOKEN }}